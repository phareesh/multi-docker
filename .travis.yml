sudo: required
services:
 - docker

before_install:
 - docker build -t phareesh/react-test -f ./client/Dockerfile.dev ./client

script:
 - docker run phareesh/react-test npm test -- --coverage

after_success:
 - docker build -t phareesh/multi-client -f ./client
 - docker build -t phareesh/multi-nginx -f ./nginx
 - docker build -t phareesh/multi-server -f ./server
 - docker build -t phareesh/multi-worker -f ./worker
 # Log in to docker CLI
 - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
 # Take those images and push them to docker hub
 - docker push phareesh/multi-client
 - docker push phareesh/multi-nginx
 - docker push phareesh/multi-server
 - docker push phareesh/multi-worker
